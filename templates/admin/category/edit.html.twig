{% extends 'admin.html.twig' %}

{% block title %}{{ 'admin.category.edit'|trans({ '%name%': category.name }) }}{% endblock %}

{% block body %}
    <div class="container mt-3">
        <h1>{{ 'admin.category.edit'|trans({ '%name%': category.name }) }}</h1>

        {{ include('admin/category/_form.html.twig', { 'button_label': 'actions.update'|trans }) }}
    </div>
{% endblock %}

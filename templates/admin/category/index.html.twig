{% extends 'admin.html.twig' %}

{% block title %}{{ 'admin.category.list'|trans }}{% endblock %}

{% block body %}
    <div class="container mt-3">
        <h1>{{ 'admin.category.list'|trans }}</h1>

        <a href="{{ path('admin_category_new') }}" class="btn btn-success btn-sm my-3 float-end">{{ 'admin.category.create'|trans }}</a>

        <table class="table table-sm table-hover">
            <thead>
                <tr>
                    <th>{{ 'admin.category.name'|trans }}</th>
                    <th>{{ 'admin.category.createdAt'|trans }}</th>
                    <th>{{ 'actions.title'|trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for category in categories %}
                    <tr>
                        <td>{{ category.name }}</td>
                        <td>{{ category.createdAt ? category.createdAt|date('d/m/Y H:i') : '' }}</td>
                        <td>
                            <div class="d-flex gap-3">
                                <a href="{{ path('admin_category_edit', {'id': category.id}) }}" class="btn btn-primary btn-sm">
                                    {{ 'actions.edit'|trans }}
                                </a>
                                {{ include('admin/category/_delete_form.html.twig') }}
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">{{ 'admin.category.empty'|trans }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}{{ 'product.list'|trans }}{% endblock %}

{% block body %}
    <div class="container my-3">
        <h1>{{ 'product.list'|trans }}</h1>

        <a href="{{ path('admin_product_new') }}" class="btn btn-success btn-sm float-end my-3">
            {{ 'product.create'|trans }}
        </a>

        <table class="table table-sm table-hover">
            <thead>
                <tr>
                    <th>{{ 'product.name'|trans }}</th>
                    <th>{{ 'product.price'|trans }}</th>
                    <th>{{ 'product.quantity'|trans }}</th>
                    <th>{{ 'product.createdAt'|trans }}</th>
                    <th>{{ 'product.updatedAt'|trans }}</th>
                    <th>{{ 'actions'|trans }}</th>
                </tr>
            </thead>
            <tbody>
            {% for product in products %}
                <tr>
                    <td>{{ product.name }}</td>
                    <td>{{ product.price }}</td>
                    <td>{{ product.quantity }}</td>
                    <td>{{ product.createdAt ? product.createdAt|date('d/m/Y H:i:s') : '' }}</td>
                    <td>{{ product.updatedAt ? product.updatedAt|date('d/m/Y H:i:s') : '' }}</td>
                    <td>
                        <div class="d-flex gap-3">
                            <a href="{{ path('admin_product_edit', {'id': product.id}) }}" class="btn btn-primary btn-sm">
                                {{ 'product.edit'|trans }}
                            </a>
                            {{ include('admin/product/_delete_form.html.twig') }}
                        </div>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">{{ 'product.empty'|trans }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

{{ form_start(form) }}
    <div class="quantity">
        <div class="d-flex gap-3">
            <div class="pro-qty">
                {{ form_row(form.quantity) }}
            </div>
            <div class="ml-4">
                {{ form_row(form.submit) }}
            </div>
        </div>
    </div>
{{ form_end(form) }}
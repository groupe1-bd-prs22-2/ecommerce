{% extends 'base.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ page.title }}</h1>
        <div class="mt-5">{{ page.content|raw }}</div>
    </div>
{% endblock %}
